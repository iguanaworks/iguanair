/* Version strings are generated by git describe and annotated tags */
#pragma once

#ifdef _DEBUG
  #define IGUANAIR_VER_STR(a) "DEBUG-${GIT_VER_STR}"
  #define IGUANAIR_REL_STR(a) "DEBUG-${GIT_REL_STR}"
#else
  #define IGUANAIR_VER_STR(a) "${GIT_VER_STR}"
  #define IGUANAIR_REL_STR(a) "${GIT_REL_STR}"
#endif
